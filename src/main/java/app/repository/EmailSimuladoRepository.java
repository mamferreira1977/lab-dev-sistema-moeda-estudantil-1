package app.repository;
import app.model.EmailSimulado;
import org.springframework.data.jpa.repository.JpaRepository;
public interface EmailSimuladoRepository extends JpaRepository<EmailSimulado, Long> { }
